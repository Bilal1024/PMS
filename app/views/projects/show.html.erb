<div class="col-md-12">
  <div class="card card-profile">
    <div class="card-body">
      <h4 class="card-title"><%= titleize(@project.name) %></h4>
      <p class="card-description">
        <%= @project.description %>
      </p>
      <%= control_links(@project) %>
    </div>
  </div>

  <div class="row">
    <%= render 'payments/payment', project: @project %>
    <%= render 'time_logs/time_log', project: @project %>
  </div>

  <div class="link">
    <%= link_to 'Back', projects_path, class: "btn btn-primary" %>
  </div>
</div>
